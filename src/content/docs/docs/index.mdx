---
title: Welcome

sidebar:
  # Set a custom label for the link
 name: Welcome
 order: 10
  # Set a custom order for the link (lower numbers are displayed higher up

---
import {baseEndpoint, host} from "@constants/api"

Welcome to Adventure Time Docs

**Base Url**: <a target="_blank" href={baseEndpoint}>{baseEndpoint}</a>

**Info URL**: <a target="_blank" href={`${host}/info`}>{`${host}/info`}</a>

The info url contains information about all available API's resources. All requests are GET requests and go over https. All responses will return data in json.

{/* NOTE: Url is generated with .env file */}